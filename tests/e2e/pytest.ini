[tool:pytest]
# Configuration for E2E tests
minversion = 6.0
addopts = 
    -v
    --strict-markers
    --strict-config
    --tb=short
    --disable-warnings
    --browser=chromium
    --headed=false
    --slowmo=0
    --video=retain-on-failure
    --screenshot=only-on-failure
    --output=test_outputs/e2e_results

markers =
    e2e: marks tests as end-to-end tests
    slow: marks tests as slow running
    websocket: marks tests that require WebSocket functionality
    ui: marks tests that test UI interactions
    api: marks tests that test API endpoints
    integration: marks tests as integration tests

testpaths = 
    tests/e2e

python_files = 
    test_*.py
    
python_classes = 
    Test*
    
python_functions = 
    test_*

# Timeout for each test (in seconds)
timeout = 300

# Directories to ignore
norecursedirs = 
    .git
    .tox
    dist
    build
    *.egg
    node_modules

# Logging configuration
log_cli = true
log_cli_level = INFO
log_cli_format = %(asctime)s [%(levelname)8s] %(name)s: %(message)s
log_cli_date_format = %Y-%m-%d %H:%M:%S

# Coverage configuration (if coverage is enabled)
# addopts = --cov=web_ui --cov-report=html --cov-report=term-missing 